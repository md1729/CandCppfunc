/*
Single quotes should be used when using "char" c1='A';
string(textual expression):c does not have a textual expression of type string.
We use a character string instead of string.
char k[20]=" Informatics World"
We use %s when outputting to the screen.

*/

#include<stdio.h>
main(){
char c[20]="World of Informatics";
//printf("%d",c);
printf("%s",c);	
}
